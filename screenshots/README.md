# Screenshots

The repo-root README references each of these by exact filename. Drop captures here from emulator/device and they'll render automatically.

Required:

- `professor_dashboard.png` — Hero gradient + present-count metrics + action cards
- `qr_attendance.png` — Premium QR card, countdown chip, present/expected tiles, lock-icon anti-cheat microcopy
- `student_success.png` — Animated SuccessCheck + "+15 points earned" gradient card + Total/Streak pills
- `live_quiz.png` — Student quiz with four large OptionCards, "Live · one answer only" chip
- `quiz_results.png` — Big correct-% hero + animated distribution bars with correct option in green
- `insight_dashboard.png` — Class-understanding hero + AI suggestion card + follow-up question
- `ai_explainer.png` — Polymorphism explained in "Like I'm 12" mode with copy button
- `leaderboard.png` — Podium card on gradient + ranked rows with current-student highlight
- `student_dashboard.png` — Hero with AnimatedCounter points + progress bar + action cards

Tips for screenshot quality:
- Take them in **light mode** unless dark mode is visibly better.
- Make sure the **Demo Mode** chip is visible — it tells judges the app is bulletproof.
- Use a stable status bar (no battery warnings, no notifications).
- Same device size across all captures.
